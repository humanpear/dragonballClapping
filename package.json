{
  "name": "dragonball-clapping-monorepo",
  "private": true,
  "version": "0.2.0",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm --parallel --filter @dragonball/web --filter @dragonball/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
